import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Injectable()
export class TokenAuthrizationService {

    constructor(private httpClient: HttpClient) {
    }
    
    GetToken() {
        
    }

    CheckToken() {
        
    }

}